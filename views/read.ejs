<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
    <link rel="stylesheet" href="./stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  </head>
  <body>
        <div class="w-full min-h-full bg-zinc-900 p-6 ">
            <div class="mb-5 px-1">
                <a href="/" class="text-blue-400 mb-4 hover:text-white transition-colors">Back to home</a>
            </div>

            <h1 class="text-3xl tracking-tighter mb-3 text-white">All Users</h1>
           <div class="users flex gap-5 flex-wrap">
           <% users.forEach((user) => { %>
                <div class="user max-w-72 p-10 bg-zinc-800 rounded-lg">
                <img src= "<%= user.image %>" alt="user image" class="w-32 h-32 rounded-full object-cover mx-auto mb-5" />
                <h2 class="text-xl text-white font-semibold mb-2 text-center"><%= user.name %> </h2>
                <p class="text-gray-300 mb-5 text-center"><%= user.email %></p>
                <div class="flex justify-between gap-2">
                    <a href="/edit/<%= user.id %>" class="rounded-md border border-gray-300 bg-blue-500 hover:bg-blue-600 text-white font-semibold cursor-pointer px-5 py-2">Edit</a>
                    <a href="/delete/<%= user.id %>" class="rounded-md border border-gray-300 bg-red-500 hover:bg-red-600 text-white font-semibold cursor-pointer px-5 py-2">Delete</a>
                </div>
            </div>
          <% }) %>
        </div>
</body>
</html>
